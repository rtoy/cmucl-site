pages:
  stage: deploy
  script:
    - (mkdir public ; cd cmucl-www/www; emacs -batch -q -l ../build.el -f wb-make)
    - ls /project/cmucl/downloads
  artifacts:
     paths:
       - public
  tags:
    - site-gen
